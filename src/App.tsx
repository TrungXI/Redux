import React, { Component } from 'react';
import { Product, Header, TaskForm } from './component';

export class App extends Component {
  render() {
    return (
      <div>
        <TaskForm/>
      </div>
    );
  }
}